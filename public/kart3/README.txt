Place your Kart3 background image here as:

  public/kart3/background.jpg

The Kart3 page uses `url('/kart3/background.jpg')` as the visual underlay for aurora/cloud animations.
If the file is missing, a gradient fallback is used so the page still works.


